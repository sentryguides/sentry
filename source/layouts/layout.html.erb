<!doctype html>
<html lang="en-us">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">
    <link rel="icon" type="image/png" href="/images/logo.png">

    <title><%= current_page.data.title || current_page.metadata[:page][:title] || "Sentry Guides" %></title>

    <%= stylesheet_link_tag "all" %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">

  </head>

  <body class="<%= page_classes %>">
    <nav class="navbar navbar-dark bg-primary">
      <div class="container">
        <a class="navbar-brand" href="/">Sentry</a>
        <ul class="nav navbar-nav">
          <li class="nav-item <% if current_page.url == "/heroes/" %>active<% end %>"><a class="nav-link" href="/heroes/">Heroes</a></li>
          <li class="nav-item <% if current_page.url == "/mechanics/" %>active<% end %>"><a class="nav-link" href="/mechanics/">Mechanics</a></li>
        </ul>
        <form class="form-inline navbar-form pull-right" role="search">
          <input type="text" class="st-default-search-input" placeholder="Search">
        </form>
      </div>
    </nav>
    <div class="container">
      <%= yield %>
      <div class="row">
        <div class="col-md-12">
          <div class="text-muted">
            Dota 2 is a registered trademark of Valve Corporation.
          </div>
        </div>
      </div>
    </div>
    <%= javascript_include_tag  "all" %>
    <% if content_for?(:scripts) %>
      <%= yield_content :scripts %>
    <% end %>
  </body>
</html>
